/**
 * Firebase Auth Test Script
 * Tests Firebase authentication (register + login)
 * 
 * NOTE: Firebase auth is client-side only. You cannot test it with a raw Node script
 * the same way Supabase could be tested. Use the browser at http://localhost:3000/register
 * 
 * For server-side testing, use Firebase Admin SDK with a service account key.
 * 
 * To test manually:
 * 1. Run: npm run dev
 * 2. Open http://localhost:3000/register → create account
 * 3. Open http://localhost:3000/login → sign in
 */

console.log('Firebase auth is browser-based. Please test via the UI at http://localhost:3000');
console.log('Register: http://localhost:3000/register');
console.log('Login:    http://localhost:3000/login');
